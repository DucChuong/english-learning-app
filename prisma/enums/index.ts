export enum Level {
  BEGINNER,
  INTERMEDIATE,
  ADVANCED,
}

export enum ProgressStatus {
  NEW,
  LEARNING,
  REVIEWING,
  MASTERED,
}

export enum ExerciseType {
  MULTIPLE_CHOICE,
  FILL_BLANK,
  MATCHING,
  LISTENING,
  SENTENCE_BUILD,
  TRUE_FALSE,
}
